---
"mobx-tanstack-query-api": patch
---

widen `GenerateQueryApiParams` types: `removeUnusedTypes` accepts `boolean` or options object (replacing narrow `false` / literal `true` in the public typings)
