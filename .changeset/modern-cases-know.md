---
"mobx-tanstack-query-api": minor
---

Add top-level `enumStyle` codegen option (`enum`, `union`, `const`) forwarded to `swagger-typescript-api` for data contracts, and apply the same styles to `Group` / `Tag` in `meta-info.ts` with matching endpoint references.
